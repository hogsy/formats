// Computer Artworks, 'Stream' header
// Template created by Mark "hogsy" Sowden <markelswo@gmail.com>

struct string
{
	uint32_t length;
	char     data[ length ];// not null-terminated
};

char          magic[ 9 ];// ARTWORKS\0
uint32_t      version;   // can be 2/3
char          unk0[ 4 ]; // always null?
char          unk1[ 4 ]; // always the same 'A193FD9B'
struct string id;
